spring:
  ai:
    mcp:
      server:
        name: pluckiermcp
        version: 1.0.0
        stdio: true
  application:
    name: mcp
  main:
    banner-mode: off
    web-application-type: servlet

logging:
  pattern:
    console: